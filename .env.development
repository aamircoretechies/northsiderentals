# Used only when running `vite` / `npm run dev` (not `vite build`).
# Point API calls at the dev server; vite.config.ts proxies `/api` → RCM API so the browser
# does not cross origins (avoids CORS while localhost is not whitelisted on the API).
VITE_API_BASE_URL=/api/v1
